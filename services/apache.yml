  apache:
    platform: linux/x86_64
    build:
      context: containers/httpd
    working_dir: /var/www/
    volumes:
      - ./source:/var/www:cached
    ports:
      - ${EXT_PORT_SHOP:-80}:80
      - ${EXT_PORT_SSL_SHOP:-443}:443